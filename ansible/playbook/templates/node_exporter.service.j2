[Unit]
Description=Node Exporter  # 서비스 설명
After=network.target  # 네트워크 서비스가 활성화된 후에 시작

[Service]
User=node_exporter  # Node Exporter 서비스를 실행할 사용자
Restart=on-failure  # 실패 시 재시작
ExecStart=/opt/node_exporter-1.3.1.linux-amd64/node_exporter  # 실행 명령

[Install]
WantedBy=default.target  # 기본 대상에서 시작
