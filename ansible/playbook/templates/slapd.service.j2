[Unit]
Description=OpenLDAP Server Daemon
After=network.target

[Service]
Type=forking
ExecStart={{ install_dir }}/libexec/slapd -f {{ install_dir }}/etc/openldap/slapd.conf

[Install]
WantedBy=multi-user.target